{
  "name": "soc-monitoring-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack SOC Detection & Alerting Platform for generating realistic Splunk/ELK security logs.",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run start:dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "start:backend": "npm start --prefix backend",
    "preview:frontend": "npm run preview --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
